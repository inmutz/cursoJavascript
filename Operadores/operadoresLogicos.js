//Operadore  Logicos
let a = true; 
let b = false;

//Operador Logico && o And
console.log(`${a} && ${b} -> ${a && b}`); 

//Operador logico || o Or
console.log(`${a} || ${b} -> ${a || b}`); 

//Operador logico ! 
console.log(`${a} -> ${!a}`)